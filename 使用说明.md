# diary 使用说明

## 第一次发布

1. 在 GitHub 创建公开仓库 `Cuiyu120/Cuiyu120.github.io`。
2. 把本目录中的所有文件推送到仓库的 `main` 分支。
3. 打开仓库的 `Settings -> Pages`。
4. 在 `Build and deployment` 的 `Source` 中选择 `GitHub Actions`。
5. 打开 `Actions`，等待 `Deploy Hexo to GitHub Pages` 完成。
6. 访问 `https://cuiyu120.github.io`。

## 本地预览

需要 Node.js 20.19 或更高版本。

```bash
npm install
npm run serve
```

浏览器打开 `http://localhost:4000`。

## 写一篇新文章

```bash
npx hexo new "文章标题"
```

文章会生成在 `source/_posts/`。编辑 Markdown 文件后运行：

```bash
npm run serve
```

确认页面后提交并推送：

```bash
git add .
git commit -m "Add a new diary entry"
git push
```

GitHub Actions 会自动重新发布网站。

## 添加照片

建议按日期保存：

```text
source/img/photos/2026-07-26/photo-01.jpg
```

文章中引用：

```markdown
![照片说明](/img/photos/2026-07-26/photo-01.jpg)
```

发布前建议把照片长边压缩到 2400 像素以内，单张控制在 2 MB 以内。

## 修改个人信息

- 网站名称、作者和网址：`_config.yml`
- 导航、头像、颜色和侧栏：`_config.butterfly.yml`
- 淡蓝色样式：`source/css/custom.css`
- 关于页面：`source/about/index.md`
- 相册页面：`source/photos/index.md`
- 头像：替换 `source/img/avatar.png`
- 首页横幅：替换 `source/img/hero.jpg`

替换图片时保持文件名不变即可，无需修改配置。

## 启用评论

模板默认关闭评论，避免未经配置的第三方服务收集访客数据。后续可以使用 Giscus，将评论保存到 GitHub Discussions；配置位置在 `_config.butterfly.yml` 的 `comments` 和 `giscus` 部分。

## 隐私提醒

GitHub Pages 是公开网站。发布照片前应检查定位、证件、票据二维码、学校或住址等敏感信息，并确认照片中的其他人同意公开。
